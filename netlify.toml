[build]
  base = "frontend"
  command = "npm ci && npm run generate"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10"
  NODE_ENV = "production"

# SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200